apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helloworld-appservice
  namespace: argocd
spec:
  project: default
  source:
    repoURL: http://helm-repo.platform-system.svc.cluster.local
    chart: helloworld-appservice
    targetRevision: 0.1.*
    helm:
      releaseName: helloworld-appservice
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
