apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-ci
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Re1lya/Markdown.git
    path: crossplane-backstage-poc/gitops/tekton
    targetRevision: main
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: ci
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
